{
  "name": "n8n-self-host-on-gcp-pulumi",
  "scripts": {
    "test": "jest --coverage",
    "test:ci": "jest --coverage --ci",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write ."
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^18",
    "jest": "^30.2.0",
    "prettier": "^3.6.2",
    "ts-jest": "^29.4.5"
  },
  "dependencies": {
    "@pulumi/gcp": "^9.4.0",
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/random": "^4.18.4",
    "typescript": "^5.9.3"
  }
}
